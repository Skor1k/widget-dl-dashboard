# Залипающая строка фильтров
**Статус:** done
**Создан:** 2026-04-15

## Цель
Сделать строку с фильтрами залипающей при скролле. PageHead (заголовок) остаётся на месте и уходит вверх, только фильтры прилипают к верху.

## Шаги
- [x] Разбить `dashboard-page__header-card` на две части: карточка с `PageHead` и отдельная строка фильтров
- [x] Добавить `position: sticky; top: 0` на `.dashboard-page__filters`
- [x] Добавить sentinel-div + `IntersectionObserver` для определения момента залипания
- [x] При залипании добавлять класс `--stuck` с `box-shadow: 0 1px 0` по нижнему краю
- [x] По умолчанию тени нет

## Контекст
- Sentinel (`height: 0`) размещён перед строкой фильтров — observer следит за ним
- `rootMargin: '-1px 0px 0px 0px'` — триггер срабатывает ровно в момент выхода за верхний край
- `gap` на `.dashboard-page` убран, отступ между фильтрами и канвасом задан через `margin-bottom: 24px` на самих фильтрах
